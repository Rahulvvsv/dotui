export { DotuiDevtools } from './devtools';
// Re-exported for consumers: typing the `schema` prop without a direct @dotui/core dep.
export type { SchemaFile } from '@dotui/core';
export type { DevtoolsData, DotuiDevtoolsProps, OverlayRecordView } from './types';
