// The overlay shape is defined React-free in @dotui/core so server/storage code can
// share it; re-exported here for convenience to runtime consumers.
export type { Overlay, StyleOverride } from '@dotui/core';
