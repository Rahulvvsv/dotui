# Copy this file to `.env` (which is git-ignored), add your key, then restart `pnpm dev`.
OPENAI_API_KEY="sk-your-openai-api-key-here"
# Optional: override the model (defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-5.4
